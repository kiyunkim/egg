var _game = (function (option) {
  var self = this,
    proto = _game.prototype,

    resources = {},
    upgrades = {},
    prestige = {};
  

  proto.init = function () {
    // ..
  };

  return { init: self.init };
});